/*  function distancia(xA, xB, xC, yA, yB, yC) {
    return Math.sqrt((xA-xB-xC)**2) + ((yA-yB-yC)**2);  
}

function quasePalindromo(palavra) {     
    for(var i = 0; i < palavra.length / 2; i++) {
        if (palavra[i] != palavra[palavra.length - i - 1]) 
        return false;
    }
    return true;
}

function repete(vetor) {
    var cont = 1;
    var num = 0;
    for(var i=1; i<vetor.length; i++) {
        cont=0;
        for(var j=0; j<vetor.length; j++) {
            if (vetor[i] == vetor[j])
            cont++;
            num = vetor[i];
        }
	    return " repeticoes numero " + num + ": " + cont + " vezes";	
	}
}

function repete(vetor) {
    var a, i, j, c = 0;
    console.log("%d", a);
    var a[] = vetor;
    for (i = 0; i < a; i++) {
        console.log("%d", vetor[i]);
    }
    for (i = 0; i < a; i++) {
         for (j = i + 1; j < a; j++) {
             if (vetor[i] == vetor[j]) {
                 c++;
             }
         }
     }
     return c;
 }*/
